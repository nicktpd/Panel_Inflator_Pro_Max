// Phase 3: selective loft painting (mask brush).
// Placeholder module — implemented in Phase 3. app.js calls initBrush()
// which currently hides the brush section entirely.

export function initBrush() {
  // Implemented in Phase 3.
  return {
    setPart() {},
    setEnabled() {},
    dispose() {},
  };
}
